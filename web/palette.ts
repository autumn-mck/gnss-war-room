export type Palette = {
	background: string;
	foreground: string;
	admin0Border: string;
	continentsBorder: string;
	cities: string;
	polarGrid: string;
	satelliteNetworks: Record<string, string>;
};
